<fest:template xmlns:fest="http://fest.mail.ru" context_name="board">
    <div class="board">

        <div class="header board-header">
            <div class="title board-header-title">
                <fest:value>board.title</fest:value>
            </div>

            <div class="member-container">
                <fest:for iterate="board.members" index="i" value="member">
                    <a class="member" href="{member.url}">
                        <img class="board-member"
                             src="{member.avatar}"
                             alt="{member.nickname}"
                             title="{member.nickname}"/>
                    </a>
                </fest:for>
                <div class="button primary" id="inviteNewMemberButton">Invite</div>
            </div>

            <div class="board-header-buttons">
                <div class="button primary" id="boardSettingsButton">Board settings</div>
            </div>
        </div>

        <div class="board-column-container">
            <fest:for iterate="board.columns" index="i" value="column">
                <div class="board-column">
                    <div class="header">
                        <div class="title board-column-title">
                            <fest:value>
                                column.title
                            </fest:value>
                        </div>
                        <div class="button board-column-settings-button" name="columnSettingsButton"/>
                    </div>


                    <div class="board-task-list">
                        <fest:for iterate="column.tasks" index="j" value="task">
                            <div class="board-task-mini" name="taskSettingsButton" data-task-id="{task.id}">
                                <div class="board-task-label-container">
                                    <fest:for iterate="task.labels" index="k" value="label">
                                        <div class="board-task-label-mini task-label-color-{label.color}">
                                            <fest:value>
                                                label.title
                                            </fest:value>
                                        </div>
                                    </fest:for>
                                </div>
                                <div class="board-task-description-mini">
                                    <fest:value>
                                        task.description
                                    </fest:value>
                                </div>
                                <div class="member-container">
                                    <fest:for iterate="task.members" index="k" value="taskMember">
                                        <a class="member" href="{taskMember.url}">
                                            <img class="board-member" src="{taskMember.avatar}"
                                                 alt="{taskMember.nickname}"
                                                 title="{taskMember.nickname}"/>
                                        </a>
                                    </fest:for>
                                </div>
                            </div>
                        </fest:for>
                        <div class="board-task-mini board-add-task" name="addNewTaskButton">
                            + Add new task
                        </div>
                    </div>
                </div>
            </fest:for>

            <div class="board-column add-board-column" id="addNewColumnButton">
                <div class="title board-column-title">
                    + Add new column
                </div>
            </div>
        </div>
    </div>
    <div id="popup-block"></div>
    <div id="popover-block"></div>
</fest:template>
